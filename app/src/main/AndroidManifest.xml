<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_chat_java"
        android:label="ChatVerse"
        android:roundIcon="@mipmap/ic_chat_java_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EjemploChat"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
        <activity
            android:name=".Chat.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".CambiarPassword"
            android:exported="false" />
        <activity
            android:name=".OlvidePassword"
            android:exported="false" />
        <activity
            android:name=".EditarInformacion"
            android:exported="false" />
        <activity
            android:name=".RegistroEmailActivity"
            android:exported="false" />
        <activity
            android:name=".LoginEmailActivity"
            android:exported="false" />
        <activity
            android:name=".OpcionesLoginActivity"
            android:exported="false" />

        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <service android:name=".Notificaciones.MyFcmService"
            android:exported="false">

            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>

            </intent-filter>

        </service>
    </application>

</manifest>